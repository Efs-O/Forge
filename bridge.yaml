#
# Run:
#   forge-llamacpp-bridge config/bridge.yaml
#
# Forge (bridge mode): apiBase http://127.0.0.1:9099/v1, apiKey = api_key below,
# model id = one of the keys under models: (referenced via bridge_config in config.yaml).

api_key: "forge-local-change-me"
bind_host: "127.0.0.1"
bind_port: 9099
# When true (default per model or here), strip channel/thought markup only if that model has think: false.
# Models with think: true are never stripped. Per-model strip_thinking_channels: false disables stripping.
strip_thinking_channels: true

# Number of llama-server processes to keep alive simultaneously.
# Each slot claims an incremental port (8080, 8081, …).
# Requires enough VRAM/RAM for all loaded models. Default: 1 (current behaviour).
max_simultaneous_models: 4

llama_server:
  binary: "N:/downloads/llama.cpp-b9402/llama-server.exe"
  host: "127.0.0.1"
  port: 8080
  n_gpu_layers: -1
  n_batch: 512
  # Concurrent worker slots per spawned llama-server (--parallel). GLOBAL: applies
  # to every model. Per-worker context = that model's num_ctx / n_parallel, so size
  # each model's num_ctx as (desired-per-worker × n_parallel). At 8, a model needs
  # num_ctx: 262144 to keep ~32k/worker; models left at 131072 run 8 × ~16k/worker.
  n_parallel: 8
  type_k: 8
  type_v: 8
  default_num_ctx: 131072            # 128k TOTAL, split across n_parallel=8 → ~16k/worker
  flash_attn_default: true

models:
  # Keys must match the client "model" id (Forge, etc.).
  gemma4-e4b-it-ud-q4kxl:
    gguf_path: "N:/GEMMA GGUF UNSLOTH/E4B/gemma-4-E4B-it-UD-Q4_K_XL.gguf"
    mmproj_path: "N:/GEMMA GGUF UNSLOTH/E4B/mmproj-F16.gguf"
    num_ctx: 131072            # 128k TOTAL across n_parallel=4 → ~32k/worker
    n_batch: 512
    flash_attn: true
    think: true
    system_prompt: |
      You are Forge, a local AI coding assistant running inside VS Code.
      You have tools available: read_file, write_file, replace_in_file, list_directory, search_code, run_terminal, git_status, git_diff, and more.
      ALWAYS use tools to accomplish tasks. Never describe what you would do - do it by calling the tools directly.
      When the user asks you to read, audit, or investigate files, call read_file immediately. Do not summarize what you plan to do.
      After each tool result, continue calling tools until the task is complete, then give a short summary.
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      top_k: 64
      min_p: 0.0
      seed: 0
      presence_penalty: 0.0
      repetition_penalty: 1.0
      repeat_last_n: 64
      stop: "<|im_end|>"
      max_tokens: 98304

  gemma4-26b-a4b-it-iq3s:
    gguf_path: "N:/GEMMA GGUF UNSLOTH/26B/gemma-4-26B-A4B-it-UD-IQ3_S.gguf"
    mmproj_path: "N:/GEMMA GGUF UNSLOTH/26B/mmproj-F16.gguf"
    num_ctx: 131072            # 128k TOTAL across n_parallel=4 → ~32k/worker
    n_batch: 512
    flash_attn: true
    think: true
    system_prompt: |
      You are Forge, a local AI coding assistant running inside VS Code.
      You have tools available: read_file, write_file, replace_in_file, list_directory, search_code, run_terminal, git_status, git_diff, and more.
      ALWAYS use tools to accomplish tasks. Never describe what you would do - do it by calling the tools directly.
      When the user asks you to read, audit, or investigate files, call read_file immediately. Do not summarize what you plan to do.
      After each tool result, continue calling tools until the task is complete, then give a short summary.
    # strip_tools: true avoids llama-server 500s when the model outputs invalid tool-call JSON, but the IDE
    # then has no tool definitions - Apply / save file / run terminal will not work. Use false for agent actions.
    strip_tools: false
    sampling:
      # Google Gemma 4 defaults (model card + HF README): temperature 1.0, top_p 0.95, top_k 64 for broad use.
      # For coding in an IDE, lower temperature reduces flippant or hallucinated code; 0.6 is a common compromise.
      # Set temperature: 1.0 to match Google evals / maximum fluency, or 0.3-0.5 for stricter, more deterministic code.
      temperature: 0.6
      top_p: 0.95
      top_k: 64
      min_p: 0.0
      seed: 0
      presence_penalty: 0.0
      repetition_penalty: 1.0
      repeat_last_n: 64
      stop: "<|im_end|>"
      # Cap for responsiveness; 26B A4B is MoE-activated - very large outputs are rarely needed for edits.
      max_tokens: 98304

  gemma4-26b-a4b-it-iq4xs:
    gguf_path: "N:/GEMMA GGUF UNSLOTH/26B/gemma-4-26B-A4B-it-UD-IQ4_XS.gguf"
    mmproj_path: "N:/GEMMA GGUF UNSLOTH/26B/mmproj-F16.gguf"
    num_ctx: 131072            # 128k TOTAL across n_parallel=4 → ~32k/worker
    n_batch: 512
    flash_attn: true
    think: true
    system_prompt: |
      You are Forge, a local AI coding assistant running inside VS Code.
      You have tools available: read_file, write_file, replace_in_file, list_directory, search_code, run_terminal, git_status, git_diff, and more.
      ALWAYS use tools to accomplish tasks. Never describe what you would do - do it by calling the tools directly.
      When the user asks you to read, audit, or investigate files, call read_file immediately. Do not summarize what you plan to do.
      After each tool result, continue calling tools until the task is complete, then give a short summary.
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      top_k: 64
      min_p: 0.0
      seed: 0
      presence_penalty: 0.0
      repetition_penalty: 1.0
      repeat_last_n: 64
      stop: "<|im_end|>"
      max_tokens: 98304

  # Dense 12B (Gemma 4 Unified, 256K ctx, text+image+audio). Sized for a DOUBLE
  # worker fleet vs the 26B: num_ctx 262144 / n_parallel 8 = ~32k/worker × 8.
  # Weights (~8.6GB at Q5) load once and are shared by all 8 slots, so only the
  # KV cache scales with total context. Swap the filename to UD-Q4_K_XL (~7.4GB)
  # for more KV headroom, or UD-Q6_K_XL (~10.7GB) for higher fidelity.
  gemma4-12b-it-ud-q5kxl:
    gguf_path: "N:/GEMMA GGUF UNSLOTH/12B/gemma-4-12b-it-UD-Q5_K_XL.gguf"
    mmproj_path: "N:/GEMMA GGUF UNSLOTH/12B/mmproj-F16.gguf"
    num_ctx: 262144            # 256k TOTAL across n_parallel=8 → ~32k/worker
    n_batch: 512
    flash_attn: true
    think: true
    system_prompt: |
      You are Forge, a local AI coding assistant running inside VS Code.
      You have tools available: read_file, write_file, replace_in_file, list_directory, search_code, run_terminal, git_status, git_diff, and more.
      ALWAYS use tools to accomplish tasks. Never describe what you would do - do it by calling the tools directly.
      When the user asks you to read, audit, or investigate files, call read_file immediately. Do not summarize what you plan to do.
      After each tool result, continue calling tools until the task is complete, then give a short summary.
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      top_k: 64
      min_p: 0.0
      seed: 0
      presence_penalty: 0.0
      repetition_penalty: 1.0
      repeat_last_n: 64
      stop: "<|im_end|>"
      max_tokens: 98304

  # Same 12B at 4-bit (~7.4GB). ~1.2GB lighter than Q5 → more KV headroom for the
  # 8-worker fleet. Test against the Q5 above for the quality/throughput tradeoff.
  gemma4-12b-it-ud-q4kxl:
    gguf_path: "N:/GEMMA GGUF UNSLOTH/12B/gemma-4-12b-it-UD-Q4_K_XL.gguf"
    mmproj_path: "N:/GEMMA GGUF UNSLOTH/12B/mmproj-F16.gguf"
    num_ctx: 262144            # 256k TOTAL across n_parallel=8 → ~32k/worker
    n_batch: 512
    flash_attn: true
    think: true
    system_prompt: |
      You are Forge, a local AI coding assistant running inside VS Code.
      You have tools available: read_file, write_file, replace_in_file, list_directory, search_code, run_terminal, git_status, git_diff, and more.
      ALWAYS use tools to accomplish tasks. Never describe what you would do - do it by calling the tools directly.
      When the user asks you to read, audit, or investigate files, call read_file immediately. Do not summarize what you plan to do.
      After each tool result, continue calling tools until the task is complete, then give a short summary.
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      top_k: 64
      min_p: 0.0
      seed: 0
      presence_penalty: 0.0
      repetition_penalty: 1.0
      repeat_last_n: 64
      stop: "<|im_end|>"
      max_tokens: 98304

  gemma4-31b-it-q3ks:
    gguf_path: "N:/GEMMA GGUF UNSLOTH/31B/gemma-4-31B-it-Q3_K_S.gguf"
    mmproj_path: "N:/GEMMA GGUF UNSLOTH/31B/mmproj-F16.gguf"
    num_ctx: 131072            # 128k TOTAL across n_parallel=4 → ~32k/worker
    n_batch: 512
    flash_attn: true
    think: true
    system_prompt: |
      You are Forge, a local AI coding assistant running inside VS Code.
      You have tools available: read_file, write_file, replace_in_file, list_directory, search_code, run_terminal, git_status, git_diff, and more.
      ALWAYS use tools to accomplish tasks. Never describe what you would do - do it by calling the tools directly.
      When the user asks you to read, audit, or investigate files, call read_file immediately. Do not summarize what you plan to do.
      After each tool result, continue calling tools until the task is complete, then give a short summary.
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      top_k: 64
      min_p: 0.0
      seed: 0
      presence_penalty: 0.0
      repetition_penalty: 1.0
      repeat_last_n: 64
      stop: "<|im_end|>"
      max_tokens: 98304

  qwen36-27b-q3km:
    gguf_path: "N:/.cache/huggingface/hub/models--unsloth--Qwen3.6-27B-GGUF/snapshots/82d411acf4a06cfb8d9b073a5211bf410bfc29bf/Qwen3.6-27B-Q3_K_M.gguf"
    num_ctx: 131072            # 128k TOTAL across n_parallel=4 → ~32k/worker
    n_batch: 512
    flash_attn: true
    think: true
    extra_llama_server_args:
      - "--reasoning-budget"
      - "8192"
    system_prompt: |
      You are Forge, a local AI coding assistant running inside VS Code.
      You have tools available: read_file, write_file, replace_in_file, list_directory, search_code, run_terminal, git_status, git_diff, and more.
      ALWAYS use tools to accomplish tasks. Never describe what you would do - do it by calling the tools directly.
      When the user asks you to read, audit, or investigate files, call read_file immediately. Do not summarize what you plan to do.
      After each tool result, continue calling tools until the task is complete, then give a short summary.
    sampling:
      # Matches --temp / --top-p / --top-k / --min-p / --presence-penalty / --repeat-penalty
      temperature: 0.6
      top_p: 0.95
      top_k: 20
      min_p: 0.0
      presence_penalty: 0.0
      repetition_penalty: 1.0
      # OpenAI field; llama-server maps this to generation cap (--n-predict)
      max_tokens: 98304
      # Qwen3.6 coding/agent mode: preserve reasoning traces across turns when thinking is enabled.
      preserve_thinking: true

  qwen36-35b-a3b-iq3s:
    gguf_path: "N:/.cache/huggingface/hub/models--unsloth--Qwen3.6-35B-A3B-GGUF/snapshots/a483e9e6cbd595906af30beda3187c2663a1118c/Qwen3.6-35B-A3B-UD-IQ3_S.gguf"
    num_ctx: 131072            # 128k TOTAL across n_parallel=4 → ~32k/worker
    n_batch: 512
    flash_attn: true
    think: true
    extra_llama_server_args:
      - "--reasoning-budget"
      - "8192"
    system_prompt: |
      You are Forge, a local AI coding assistant running inside VS Code.
      You have tools available: read_file, write_file, replace_in_file, list_directory, search_code, run_terminal, git_status, git_diff, and more.
      ALWAYS use tools to accomplish tasks. Never describe what you would do - do it by calling the tools directly.
      When the user asks you to read, audit, or investigate files, call read_file immediately. Do not summarize what you plan to do.
      After each tool result, continue calling tools until the task is complete, then give a short summary.
    sampling:
      temperature: 0.6
      top_p: 0.95
      top_k: 20
      min_p: 0.0
      presence_penalty: 0.0
      repetition_penalty: 1.0
      max_tokens: 98304
      # Qwen3.6 coding/agent mode: preserve reasoning traces across turns when thinking is enabled.
      preserve_thinking: true

  qwen36-27b-mtp-q3km:
    gguf_path: "N:/.cache/huggingface/models--unsloth--Qwen3.6-27B-MTP-GGUF/snapshots/5cb35eb3dcbf52dbce5f87dbc64df6aaffadcace/Qwen3.6-27B-Q3_K_M.gguf"
    num_ctx: 131072            # 128k TOTAL across n_parallel=4 → ~32k/worker
    n_batch: 512
    flash_attn: true
    think: true
    extra_llama_server_args:
      - "--reasoning-budget"
      - "8192"
      - "--spec-type"
      - "draft-mtp"
      - "--spec-draft-n-max"
      - "2"
    system_prompt: |
      You are Forge, a local AI coding assistant running inside VS Code.
      You have tools available: read_file, write_file, replace_in_file, list_directory, search_code, run_terminal, git_status, git_diff, and more.
      ALWAYS use tools to accomplish tasks. Never describe what you would do - do it by calling the tools directly.
      When the user asks you to read, audit, or investigate files, call read_file immediately. Do not summarize what you plan to do.
      After each tool result, continue calling tools until the task is complete, then give a short summary.
    sampling:
      temperature: 0.6
      top_p: 0.95
      top_k: 20
      min_p: 0.0
      presence_penalty: 0.0
      repetition_penalty: 1.0
      max_tokens: 98304
      preserve_thinking: true

  qwen36-35b-a3b-mtp-iq3s:
    gguf_path: "N:/.cache/huggingface/models--unsloth--Qwen3.6-35B-A3B-MTP-GGUF/snapshots/5bc3e238d916f48a861bac2f8a1990a0e9b7e98d/Qwen3.6-35B-A3B-UD-IQ3_S.gguf"
    num_ctx: 131072            # 128k TOTAL across n_parallel=4 → ~32k/worker
    n_batch: 512
    flash_attn: true
    think: true
    extra_llama_server_args:
      - "--reasoning-budget"
      - "8192"
      - "--spec-type"
      - "draft-mtp"
      - "--spec-draft-n-max"
      - "2"
    system_prompt: |
      You are Forge, a local AI coding assistant running inside VS Code.
      You have tools available: read_file, write_file, replace_in_file, list_directory, search_code, run_terminal, git_status, git_diff, and more.
      ALWAYS use tools to accomplish tasks. Never describe what you would do - do it by calling the tools directly.
      When the user asks you to read, audit, or investigate files, call read_file immediately. Do not summarize what you plan to do.
      After each tool result, continue calling tools until the task is complete, then give a short summary.
    sampling:
      temperature: 0.6
      top_p: 0.95
      top_k: 20
      min_p: 0.0
      presence_penalty: 0.0
      repetition_penalty: 1.0
      max_tokens: 98304
      preserve_thinking: true

  # ---------------------------------------------------------------------------
  # Ollama models (provider: ollama). Forge sends each YAML key as OpenAI API
  # "model"; use the exact tag from https://ollama.com/library for that variant.
  # Local `ollama list` often does NOT enumerate everything the server can run:
  # library / cloud routes may resolve on first chat request (pull or upstream),
  # so absence from `ollama list` does not prove a tag is invalid.
  # ---------------------------------------------------------------------------

  gemma4:e2b:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 128K context window. Note: Forge currently talks to Ollama via
    # /v1/chat/completions, which cannot set num_ctx per request; this is descriptive
    # unless the underlying Ollama model was created with the matching Modelfile setting.
    num_ctx: 131072
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  gemma4:26b:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 256K context window.
    num_ctx: 262144
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  gemma4:31b:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 256K context window.
    num_ctx: 262144
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  gemma4:e4b:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 128K context window.
    num_ctx: 131072
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  qwen3.5:4b:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 256K context window.
    num_ctx: 262144
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  qwen3.5:9b:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 256K context window.
    num_ctx: 262144
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  # Cloud / upstream models (same tag as on ollama.com; usually not visible in
  # `ollama list` until routed or synced — Forge still forwards the tag correctly).
  deepseek-v4-flash:cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 1M context window.
    num_ctx: 1000000
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 65536

  deepseek-v4-pro:cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 1M context window.
    num_ctx: 1000000
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 65536

  kimi-k2.6:cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 256K context window.
    num_ctx: 262144
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  glm-4.6:cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 198K context window.
    num_ctx: 198000
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  glm-5.1:cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 198K context window.
    num_ctx: 198000
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  gemma4:31b-cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 256K context window.
    num_ctx: 262144
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  qwen3.5:397b-cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 256K context window.
    num_ctx: 262144
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 65536

  qwen3-coder:480b-cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 256K context window.
    num_ctx: 262144
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  qwen3-vl:235b-cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 256K context window.
    num_ctx: 262144
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 65536

  qwen3-vl:235b-instruct-cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 256K context window.
    num_ctx: 262144
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 65536

  gpt-oss:120b-cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    num_ctx: 131072
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  gpt-oss:20b-cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    num_ctx: 131072
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072

  deepseek-v3.1:671b-cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    # Ollama library: 160K context window.
    num_ctx: 160000
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 32768

  minimax-m2.7:cloud:
    provider: ollama
    endpoint: "http://127.0.0.1:11434"
    num_ctx: 200000
    think: true
    reasoning_effort: medium
    strip_tools: false
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 131072


  # ---------------------------------------------------------------------------
  # xAI / Grok models (provider: xai). Auth via VS Code SecretStorage —
  # run "Forge: Set Cloud Provider Token", key name "xai", then paste your
  # xAI bearer token. This uses an OpenAI-compatible API shape, but it is not
  # the same credential as an OpenAI API key. No local backend needed; Forge
  # calls https://api.x.ai directly.
  # ---------------------------------------------------------------------------

  grok-build-0.1:
    provider: xai
    api_key_secret: xai
    capabilities: [tool-call]
    num_ctx: 256000
    sampling:
      temperature: 0.7
      max_tokens: 65536

  grok-4.3:
    provider: xai
    api_key_secret: xai
    capabilities: [tool-call]
    num_ctx: 1000000
    sampling:
      temperature: 0.7
      max_tokens: 30000

  grok-4.20-0309-non-reasoning:
    provider: xai
    api_key_secret: xai
    capabilities: [tool-call]
    num_ctx: 2000000
    sampling:
      temperature: 0.7
      max_tokens: 30000

  grok-4.20-0309-reasoning:
    provider: xai
    api_key_secret: xai
    capabilities: [tool-call]
    think: true
    num_ctx: 2000000
    sampling:
      temperature: 0.7
      max_tokens: 30000

  # No tool_call support — chat only
  grok-4.20-multi-agent-0309:
    provider: xai
    api_key_secret: xai
    strip_tools: true
    num_ctx: 2000000
    sampling:
      temperature: 0.7
      max_tokens: 30000

  # ---------------------------------------------------------------------------
  # OpenRouter models (provider: openrouter). OpenAI-compatible cloud gateway.
  # Auth via VS Code SecretStorage — run "Forge: Set Cloud Provider Token",
  # key name "openrouter", paste your key from https://openrouter.ai/keys.
  # No local backend; Forge calls https://openrouter.ai/api/v1 directly.
  # Model ids: copy the exact slug from https://openrouter.ai/models — free
  # variants carry the ":free" suffix and share ONE account-wide rate limit
  # (~20 req/min + a daily cap), so route only heavy jobs here. The YAML key IS
  # the slug sent to OpenRouter, so it must match exactly (a wrong slug = 404).
  # Slugs below verified against https://openrouter.ai/api/v1/models on
  # 2026-06-02 (all support tool-calling); free models come and go, so re-check
  # if a model starts returning 404.
  # ---------------------------------------------------------------------------

  nvidia/nemotron-3-super-120b-a12b:free:
    provider: openrouter
    api_key_secret: openrouter
    capabilities: [tool-call, long-context]
    num_ctx: 262144
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 16384

  moonshotai/kimi-k2.6:free:
    provider: openrouter
    api_key_secret: openrouter
    capabilities: [tool-call, long-context]
    num_ctx: 262144
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 16384

  poolside/laguna-m.1:free:
    provider: openrouter
    api_key_secret: openrouter
    capabilities: [tool-call, long-context]
    num_ctx: 262144
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 16384

  google/gemma-4-31b-it:free:
    provider: openrouter
    api_key_secret: openrouter
    capabilities: [tool-call, long-context]
    num_ctx: 262144
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 16384

  google/gemma-4-26b-a4b-it:free:
    provider: openrouter
    api_key_secret: openrouter
    capabilities: [tool-call, long-context]
    num_ctx: 262144
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 16384

  # Paid route (no :free) — draws on your OpenRouter credit, skips the shared
  # free-tier rate limit. Confirm the slug + price at https://openrouter.ai/models
  # (a slug with no paid provider returns 404).
  google/gemma-4-26b-a4b-it:
    provider: openrouter
    api_key_secret: openrouter
    capabilities: [tool-call, long-context]
    num_ctx: 262144
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 16384

  # ---------------------------------------------------------------------------
  # OpenAI-compatible providers:
  # - provider: openai uses https://api.openai.com
  # - provider: openai-compatible uses your custom endpoint
  # Both require api_key_secret and the token can be stored with
  # "Forge: Set Cloud Provider Token".
  # ---------------------------------------------------------------------------

  codex:
    provider: openai
    api_key_secret: openai
    capabilities: [tool-call, long-context]
    num_ctx: 400000
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 16384

  claude-via-gateway:
    provider: openai-compatible
    endpoint: https://your-openai-compatible-gateway.example.com
    api_key_secret: gateway
    capabilities: [tool-call, long-context]
    num_ctx: 200000
    sampling:
      temperature: 0.6
      top_p: 0.95
      max_tokens: 12000

